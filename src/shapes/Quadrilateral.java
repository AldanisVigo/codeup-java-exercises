package shapes;

public abstract class Quadrilateral extends Shape implements Measurable {
    //Inside your shapes' directory, create a class named Rectangle. It should have protected properties for both
    //length and width.
    protected double length;
    protected double width;
}
